# Development and experimentation dependencies: Jupyter notebooks
# (src/fnc_vs_fnc_test.ipynb), plotting and the auxiliary scripts such as
# src/binutils_test.py. These are NOT required to run the MANTILLA tool itself.
#
# Install with: python3 -m pip install -r requirements-dev.txt
# (this also pulls in the runtime dependencies below).
#
# NOTE: the security-patched Jupyter stack and nltk below require Python >= 3.10.
# The runtime requirements.txt remains compatible with Python >= 3.9.

-r requirements.txt

# Experimentation / analysis
nltk>=3.9.4
matplotlib>=3.9.2
eli5>=0.13.0
graphviz>=0.20.3
tabulate>=0.9.0

# Jupyter stack. Minimum versions pinned to releases that fix known
# security advisories flagged by Dependabot.
jupyter>=1.1.1
jupyterlab>=4.5.7
notebook>=7.5.6
jupyter-server>=2.20.0
nbconvert>=7.17.1
mistune>=3.2.1
bleach>=6.4.0
tornado>=6.5.6
pillow>=12.2.0
fonttools>=4.60.2
h11>=0.16.0
Pygments>=2.20.0
jinja2>=3.1.6
requests>=2.33.0
urllib3>=2.7.0
idna>=3.15
