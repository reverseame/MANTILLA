# Runtime dependencies for MANTILLA (MANTILLA.py and feature_extraction.py).
# Development/experimentation dependencies (Jupyter, plotting, etc.) live in
# requirements-dev.txt.
#
# Minimum versions are pinned to releases that fix known security advisories.

numpy>=1.24.3
pandas>=2.2.3
scikit-learn>=1.5.0
python-magic>=0.4.27
r2pipe>=1.9.4
python-tlsh>=4.5.0
ssdeep>=3.4
